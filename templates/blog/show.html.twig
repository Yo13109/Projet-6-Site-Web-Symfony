{% extends "base.html.twig" %}
{% form_theme formComment 'bootstrap_4_layout.html.twig' %}

{% block body %}


	<div class="card  mb-3 mt-5 ">
		<section class="Figure">
			<div class="card-header">
				<h2>{{trick.name}}</h2>
				<div class="metadata">
					Ecrit le
					{{trick.createdate | date('d/m/y')}}
					à
					{{trick.createdate| date('H:i')}}
					dans la catégorie
					{{trick.category.name}}
				</div>
			</div>
			<div class="card-content">
				<img src="" alt="">
				{{trick.content}}

			</div>
		</section>
		<div class="card-header"></div>
		<div class="col-lg-6 offset-lg-3 col-md-12">
			<div class="card  mt-5">
				<div class="card-header">


					<h3>
						Ajout d'un commentaire
					</h3>

				</div>
				<div class="card-content mb-5">
					{{ form_start(formComment) }}
					{{ form_row(formComment.content,{'attr':{'placeholder':"Ecrire un commentaire..."}}) }}

				</div>

				<button type="submit" class="btn btn-success">Ajouter un commentaire</button>
				{{ form_end(formComment) }}
			</div>
		</div>

		<div class="col-lg-6 offset-lg-3 col-md-12">
			<div class="card  mt-5">
				<div class="card-header mb-4">


					<h3>
						Commentaire
					</h3>

				</div>
				<div class="card-content">

					{% for comment in comment %}


						<div class="comment">
							<div class="container">

								<div class="row">

									{{comment.user.avatar}}

								<div class= 'date col-3 offset-lg-8'>
									{{comment.date | date('d/m/y')}} à {{comment.date | date('H:i')}}
								</div>

								</div>


								<div class="content col-12 mt-4">{{comment.content}}
								</div>
								<div class="button col-2 offset-lg-10"></div>


							</div>

						</div>
					{% endfor %}


				</div>
			</div>
		</div>
	</div>

{% endblock %}
