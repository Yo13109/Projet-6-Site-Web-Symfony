{% extends "base.html.twig"  %}


{% block body %}


	<img src="https://snowtricks.jeandescorps.fr/images/backflip.jpg" width="1519" height="800" alt="">
	<div class="container">
		<div class="row mt-4 mb-4">
			{% for trick in tricks %}


				<div class="  col-lg-3 col-md-12">

					<div class="card mb-4">


						<div class="card-content">la photo</div>


						<div class="card-footer">

							<a href="{{path('show_figure', {'id': trick.id})}}" class="btn btn-primary">
								<h6>{{trick.name}}</h6>
							</a>
							<div class="name text-right">
								<a href="{{path('update_figure', {'id':trick.id})}}">
									<i class="far fa-edit right"></i>
								</a>
								<a href="">
									<i class="far fa-trash-alt right"></i>
								</a>
							</div>
						</div>

					</div>

				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
