<form method="post" action="{{ path('app_task_delete', {'id': video.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette video?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video.id) }}">
    <button class="btn">Delete</button>
</form>